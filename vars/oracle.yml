---
ORACLE_BASE:        "{{ oracle_base }}"
ORACLE_HOME:        "{{ oracle_home }}"
INVENTORY_LOCATION: "{{ oracle_inventory }}"
INSTALL_TYPE:       "{{ oracle_installation }}"
SELECTED_LANGUAGES: "{{ oracle_languages }}"
UNIX_GROUP_NAME:    "{{ oracle_group }}"
